# Local RAG System 校招面试准备手册

> 最后核对日期：2026-07-31
> 用途：帮助项目作者从“会运行项目”提升到“能清楚解释项目、设计取舍、迭代过程和不足”。
> 阅读顺序：先背“30 秒版”和“3 分钟版”，再理解两条主链路，最后练习高频问答。
> 维护规则：这不是每次代码变化都自动更新的文档。只有明确要求“更新校招准备文件”时，才需要重新阅读 DevLog 和当前代码后更新。

---

## 〇、先记住这 12 句话

如果时间很少，先把下面 12 句话说顺：

1. 这是一个本地优先的 RAG 知识问答系统，解决通用大模型不了解私有文档、回答缺少证据和可能产生幻觉的问题。
2. 系统把 PDF、TXT、Markdown、DOCX 加载后切成 chunk，批量生成 embedding，再写入 Qdrant。
3. 查询时不会直接把问题交给生成模型，而是先做路由、意图识别和查询改写。
4. 问候语通过正则直接回复，普通闲聊可以直接回答，文档问题才进入向量检索，所以不同问题的成本不同。
5. 检索默认取 5 个 chunk；开启 Rerank 后先召回约 20 个候选，再用 Cross-Encoder 或轻量融合方法精排到 5 个。
6. 最终生成模型只根据检索上下文和会话记忆回答，并把来源、页码、分数等信息返回前端。
7. SSE 流式接口按 routing、status、token、sources、done/error 发送事件，前端可以显示搜索、精排和生成进度。
8. 文档摄入支持 MD5 增量更新：未变化文件跳过，变化文件先删除旧向量，再重新切分和写入。
9. 会话不仅保存到 SQLite，还采用“滚动摘要 + 最近原文”的两级记忆，`conversation_id` 会在服务端恢复上下文。
10. 上下文预算统一考虑系统提示、当前问题、历史、文档、输出预留和安全余量，无法容纳的问题会明确报错，而不是等模型崩溃。
11. 项目有独立 Retrieval 评估体系，用 Recall@K、Precision@K、MRR、NDCG 和上下文冗余率比较 chunk、embedding 和 Rerank 策略。
12. 这个项目的价值不只是“调了一个 LangChain API”，而是把摄入、检索、生成、评估、流式交互、持久化、安全和回归测试串成了完整工程闭环。

---

## 一、项目到底是干什么的

### 1.1 一句话定义

这是一个支持本地模型和云模型的私有文档 RAG 系统：用户上传或批量摄入文档，系统将文档向量化存入 Qdrant；用户提问时，系统先检索相关证据，再让大模型结合证据和对话记忆生成答案。

### 1.2 它解决了什么问题

直接使用通用大模型有三个典型问题：

- 模型不知道企业内部文档、个人笔记、项目资料等私有知识；
- 模型知识可能过时，无法跟随本地文件更新；
- 模型可能凭参数记忆“猜答案”，缺乏可追溯证据。

RAG 的解决办法是：

```text
用户问题
   ↓
从私有知识库检索相关文档
   ↓
把检索证据和问题一起交给 LLM
   ↓
生成有依据、可显示来源的回答
```

### 1.3 为什么选择本地优先

本地优先主要考虑：

- 隐私：文档和聊天记录不必离开本地环境；
- 成本：本地推理不按 token 收费；
- 可控：模型、embedding、向量库和上下文窗口均可配置；
- 兼容：统一采用 OpenAI-compatible API，所以 LM Studio、Ollama 和云服务可以复用同一业务代码。

### 1.4 项目边界

它不是：

- 一个训练大模型的项目；
- 一个完整的多租户 SaaS；
- 一个 Agent 自动执行系统；
- 一个已经达到商业级检索质量的搜索产品。

它是：

- 一个完整的 RAG 工程原型；
- 一个能展示后端、前端、向量检索、LLM、评估和工程可靠性的校招项目；
- 一个可以继续扩展权限、混合检索、可观测性和生成质量评估的平台。

---

## 二、面试开场怎么说

### 2.1 30 秒版本

> 我做了一个本地优先的 RAG 知识问答系统。文档经过加载、分块和批量 embedding 后写入 Qdrant，查询时先经过两层路由和查询改写，再做向量召回、可选 Rerank 和 LLM 流式生成。系统还支持 MD5 增量摄入、SQLite 会话持久化、滚动摘要长期记忆、统一 token 上下文预算，以及一套包含 Recall、Precision、MRR、NDCG 的离线检索评估体系。前后端分别使用 FastAPI 和 React，LLM 与 embedding 通过 OpenAI-compatible API 接入本地 LM Studio 或云服务。

### 2.2 1 分钟版本

> 这个项目主要解决大模型无法可靠回答私有文档问题的问题。摄入侧支持 PDF、TXT、Markdown 和 DOCX，Markdown 会先按标题层级切分，再递归切分；所有 chunk 批量生成 embedding 后写入 Qdrant，同时使用 SQLite MD5 状态库实现增量更新。查询侧先用正则拦截问候等简单请求，再用一次 LLM 同时判断是否需要 RAG、识别意图并改写检索词。需要检索时先向量召回，可配置 Cross-Encoder 或向量加关键词融合的 Rerank，然后将最相关文档、当前问题和服务端会话记忆送给生成模型。回答通过 SSE 逐 token 返回，并展示来源。后来我又补了长期上下文机制：服务端按 conversation_id 恢复完整会话，把旧消息压缩为滚动摘要，保留近期原文，并对整个提示词做统一 token 预算。最后通过 22 条 golden question 和多种检索指标做离线评估，而不是只凭主观感觉调参数。

### 2.3 3 分钟版本

> 项目的核心可以分成摄入流和查询流。
>
> 摄入流负责把文件变成可搜索的向量。文件首先经过扩展名校验并以 UUID 安全落盘，然后根据格式选择加载器：PDF 使用 PyPDFLoader，文本和 Markdown 使用 TextLoader，DOCX 使用 Docx2txtLoader。Markdown 会先按 H1/H2/H3 保留章节层级，再用 RecursiveCharacterTextSplitter 控制 chunk 大小；其他格式直接递归切分。之后系统通过 OpenAI-compatible embedding 接口按 64 条一批生成向量，并按 500 个 point 一批写入 Qdrant，避免 32MB payload 限制。摄入状态记录在 SQLite 中，以文件路径和 MD5 判断新增、变化或未变化，实现幂等和增量更新。
>
> 查询流先恢复服务端会话记忆，然后做上下文预算。路由分两层：第一层是正则规则，问候、致谢等请求零模型调用；第二层由 LLM 一次完成是否需要检索、意图识别以及查询改写或直接回答。需要 RAG 时，改写后的问题用于 embedding 和 Qdrant 相似度检索，原始问题仍用于最终回答，防止改写改变用户意图。默认直接取 top-5；开启 Rerank 时先召回 20 个候选，再用 Cross-Encoder 或轻量 Hybrid Fusion 排到 5 个。最后把来源化的文档上下文、长期摘要、近期原文和当前问题送入生成模型。
>
> 前端通过 SSE 接收 routing、status、token、sources 和 done/error，因此用户在模型首 token 前也能看到“正在搜索、正在精排、正在生成”。每轮问答异步保存到 SQLite，完整原始消息用于 UI 和审计，旧消息则批量压缩成滚动摘要，模型侧保留最近 12 条原文。统一上下文管理器会扣除系统提示、问题、输出预留和安全余量，再动态分配历史和文档预算。
>
> 为避免项目只停留在“能跑”，我还实现了 Retrieval 离线评估：golden dataset 使用稳定的 file_path 和 text snippet 标注，而不是容易随 chunk 策略变化的 chunk index；指标包括 Recall@K、Precision@K、MRR、NDCG 和上下文冗余率，可以比较不同 chunk、embedding 和 Rerank 策略。当前结果也表明 Rerank 不是必然提升，需要用实验决定是否开启。

---

## 三、整体架构

### 3.1 技术栈

| 层级 | 技术 | 作用 |
|------|------|------|
| 前端 | React 19 + Vite | 聊天、上传、文档管理、历史会话、SSE 渲染 |
| 后端 | FastAPI + Uvicorn | HTTP API、参数校验、业务编排、SSE |
| RAG 编排 | LangChain | Loader、Splitter、Prompt、LLM/Embedding 封装 |
| 向量数据库 | Qdrant | 保存向量和 chunk payload、相似度检索 |
| LLM | LM Studio/Ollama/云端兼容 API | 路由、改写、摘要、最终生成 |
| Embedding | OpenAI-compatible embedding API | 查询和文档向量化 |
| Rerank | sentence-transformers CrossEncoder / Hybrid Fusion | 对向量召回候选精排 |
| 状态存储 | SQLite | 摄入 checksum、聊天记录、滚动摘要游标 |
| 评估 | unittest + pandas/matplotlib/seaborn/Jupyter | 离线指标、实验对比、回归测试 |

### 3.2 宏观模块关系

```text
                           ┌────────────────────┐
                           │   React Frontend   │
                           │ Chat / Upload / SSE│
                           └─────────┬──────────┘
                                     │ HTTP / SSE
                           ┌─────────▼──────────┐
                           │      FastAPI       │
                           │ API + Schema 校验  │
                           └─────────┬──────────┘
                 ┌───────────────────┼────────────────────┐
                 │                   │                    │
        ┌────────▼────────┐ ┌────────▼────────┐ ┌────────▼────────┐
        │ Ingestion Flow  │ │   Query Flow    │ │ Conversation API│
        │ load/split/embed│ │ route/retrieve  │ │ list/get/delete │
        └────────┬────────┘ │ rerank/generate │ └────────┬────────┘
                 │          └────────┬────────┘          │
        ┌────────▼────────┐          │          ┌────────▼────────┐
        │     Qdrant      │◄─────────┘          │     SQLite      │
        │ vectors/payload │                     │ chat + summary  │
        └─────────────────┘                     └─────────────────┘
                 ▲
                 │
        ┌────────┴────────┐
        │ Evaluation      │
        │ golden + metrics│
        └─────────────────┘
```

### 3.3 分层原则

项目大体遵循：

```text
API 层 → Service 编排层 → RAG/Storage 原语层
```

- API 层负责 HTTP、Pydantic、状态码和 StreamingResponse；
- Service 层负责把多个步骤串起来；
- RAG 层负责可复用的单一能力，如加载、切分、检索、精排、上下文预算；
- Store/Vectorstore 负责数据访问；
- 前端不直接理解 Qdrant 或模型，只理解 API 和 SSE 协议。

面试可以说：这种分层使测试时能 mock 外部边界，而不用启动 Qdrant 和模型。

---

## 四、摄入链路：文档如何进入知识库

### 4.1 完整流程

```text
文件/目录
  ↓
扩展名校验
  ↓
计算 MD5，与 ingestion_state.db 对比
  ├─ 未变化：跳过
  ├─ 新文件：继续
  └─ 已变化：先删 Qdrant 旧 chunks，再继续
  ↓
Loader 解析为 LangChain Document
  ↓
注入 source/file_path/file_name/file_type/md5 metadata
  ↓
Splitter 切成 chunks
  ↓
批量 Embedding（默认 64 条/请求）
  ↓
批量 Qdrant Upsert（500 points/批）
  ↓
更新 SQLite checksum 状态
```

### 4.2 两个摄入入口为什么共用一条 Pipeline

入口有两个：

- Web 上传：`POST /documents/upload` 和 `/upload-batch`；
- CLI 批量目录：`python ingest.py --input_dir ...`。

它们最终都委托给 `ingest_file_paths()` 或同一模块中的 `ingest_directory()`。

这样做的好处：

- 不会出现“上传 API 一套切分规则，CLI 又是另一套”的漂移；
- 两种入口都获得 MD5 增量检测；
- 两种入口都使用同样的 embedding 和 Qdrant payload；
- 修复摄入 bug 时只需要改一处。

### 4.3 文件加载策略

| 文件类型 | Loader | 说明 |
|---------|--------|------|
| PDF | `PyPDFLoader` | 通常按页产生 Document，metadata 可包含 page |
| TXT | `TextLoader` | UTF-8 纯文本 |
| MD/Markdown | `TextLoader` | 后续使用标题感知切分 |
| DOCX | `Docx2txtLoader` | 提取 Word 文本 |

当前不支持图片 OCR、扫描 PDF、Excel、PPT 和网页抓取。

### 4.4 为什么要切分

整篇文档直接 embedding 有三个问题：

- 文档可能超过 embedding 模型限制；
- 一个向量表示多个主题，语义被平均；
- 检索后整篇塞入 LLM，token 成本和噪声过高。

切分的目标不是越小越好，而是平衡：

- 粒度小：定位精确、召回可能更高，但上下文容易不完整；
- 粒度大：语义完整，但一个 chunk 可能包含多个主题、精度下降；
- overlap：减少边界信息丢失，但会增加存储和重复。

当前默认：

```text
chunk_size = 800
chunk_overlap = 120
```

这里是 RecursiveCharacterTextSplitter 的字符尺度，不等同于严格 token。

### 4.5 Markdown 为什么单独处理

Markdown 自带章节结构。如果只按固定字符切，可能把标题和正文分开。

当前策略：

1. 先用 `MarkdownHeaderTextSplitter` 按 H1/H2/H3 切；
2. `strip_headers=False`，标题仍保留在文本中；
3. 把 h1/h2/h3 写入 metadata；
4. 对过大的章节再用递归切分器细分。

这让检索到的 chunk 带有章节语义，比完全无结构切分更适合工程文档。

### 4.6 RecursiveCharacterTextSplitter 的分隔优先级

```python
["\n\n", "\n", "。", ".", " ", ""]
```

含义是优先在自然段、换行、中文句号、英文句号和空格处分割，最后才退化到字符级分割。

### 4.7 增量更新为什么使用 MD5

系统需要判断文件是否变化。MD5 在这里不是密码学用途，而是内容指纹：

- 计算快；
- 相同内容能稳定得到相同结果；
- 足以判断本地文件是否需要重新 embedding。

状态保存到 `data/ingestion_state.db`，主要记录：

- file_path；
- md5；
- last_ingested_at；
- chunk_count；
- collection_name。

### 4.8 为什么变化文件要“先删后写”

如果文件从 20 个 chunk 变为 15 个，只做 upsert 会遗留 5 个旧 chunk。

所以变化文件执行：

```text
按 metadata.file_path 删除旧 points
  ↓
重新 load/split/embed/upsert
```

### 4.9 批量 Embedding

文档 chunk 不逐条请求，而是默认每 64 条组成一个 embeddings 请求。

好处：

- 减少 HTTP 往返；
- 更好利用 embedding 服务的批处理能力；
- 大目录摄入速度明显优于逐条调用。

响应会按 `response.data[].index` 排序，确保向量顺序和 chunk 顺序一致。

### 4.10 Qdrant 批量写入

Qdrant 单请求 payload 有大小限制。项目在真实摄入中遇到过 809 points、约 44.8MB 导致 400 的问题，因此改成：

```text
UPSERT_BATCH_SIZE = 500
```

如果 collection 不存在，会从第一个 embedding 推断向量维度并自动创建，距离函数使用 cosine。

Point ID 使用 UUID5，从 file_path、MD5 和顺序构造，目标是让相同输入获得稳定 ID。

### 4.11 摄入链路可以怎么背

口诀：

> 扫描、校验、判变更；加载、切分、批向量；分批写库、记录状态。

---

## 五、查询链路：一次问题经历了什么

### 5.1 完整流程

```text
当前问题 + conversation_id + 客户端近期尾部
  ↓
服务端恢复 SQLite 摘要与近期原文
  ↓
路由阶段上下文预算
  ↓
Layer 0 正则快速路由
  ├─ 问候/感谢/告别/元问题 → 预设快速回复
  └─ 未命中
       ↓
Layer 1 LLM：路由 + 意图 + 改写/直接回答
  ├─ ROUTING=NO → direct answer
  └─ ROUTING=YES
       ↓
改写查询生成 embedding
       ↓
Qdrant similarity search
       ↓
可选 Rerank
       ↓
统一最终上下文预算
       ↓
RAG Prompt + LLM
       ↓
答案 + Sources + Routing
       ↓
异步日志 + SQLite 保存 + 可选滚动摘要
```

### 5.2 为什么先做 Query Processing

用户真实提问经常不适合直接检索：

- “它是怎么实现的？”缺少指代对象；
- “比较一下这两个方案”没有展开关键词；
- “帮我总结一下”缺少明确文档主题；
- 问候语根本不需要查向量库。

Query Processor 同时完成：

- `needs_rag`：是否需要知识库；
- `intent`：question_answering / summarization / comparison / fact_lookup；
- `rewritten_query`：更适合 embedding 的检索词；
- `direct_answer`：不需要检索时直接回答。

### 5.3 为什么改写查询用于检索，原问题用于生成

改写后的查询通常更关键词化，更适合向量检索，但可能丢失用户语气或细节。

因此：

```text
rewritten_query → 检索
original question → 最终回答
```

这样兼顾召回效果与回答忠实度。

### 5.4 两层路由门控

#### Layer 0：规则层

通过正则匹配：

- greeting：hello、你好等；
- thanks：thanks、谢谢等；
- goodbye：bye、再见等；
- meta：你是谁、你能做什么等。

优点：

- 零 LLM 调用；
- 延迟极低；
- 输出稳定；
- 不浪费向量检索。

缺点：

- 规则覆盖有限；
- 复杂表达不能靠正则判断。

#### Layer 1：LLM 层

未命中规则时，一次 LLM 调用同时完成路由、意图和改写/回答，避免拆成三次模型调用。

### 5.5 三条查询路径与成本

| 路径 | 例子 | LLM 调用 | 向量检索 |
|------|------|---------:|---------|
| greeting | “你好” | 0 | 否 |
| direct | 明确不需要文档的闲聊/元问题 | 1 | 否 |
| rag | 项目、文档、事实、总结、对比 | 通常 2 | 是 |

注意：路由模型失败时当前策略是 fail-open：

```text
needs_rag = True
rewritten_query = 原问题
```

原因是对 RAG 系统来说，误检一次通常比错误跳过知识库更安全。

### 5.6 向量检索原理

Embedding 将文本映射为高维向量。语义相似文本在向量空间距离更近。

摄入时：

```text
document chunk → embedding vector → Qdrant
```

查询时：

```text
rewritten query → embedding vector
                    ↓
Qdrant 找最近邻 chunk
```

当前使用 QdrantVectorStore 的 `similarity_search_with_score`，默认 top-k 为 5。

### 5.7 查询 Embedding 缓存

系统为重复查询 embedding 设置 256 条线程安全内存缓存，key 是问题文本的 MD5。

需要注意当前实现细节：

- 命中时直接返回向量；
- 超过 256 条时删除字典中最早插入项；
- 命中不会把条目移动到末尾。

所以虽然类名和注释写的是 LRU-style，当前行为更准确地说是 FIFO-like，而不是严格 LRU。面试中如果被问，可以主动指出并说下一步会用 `OrderedDict.move_to_end()` 或 `functools.lru_cache` 修正。

### 5.8 `@rag` 强制检索

前端识别 `@rag`：

- UI 展示和发送给模型的问题会去掉前缀；
- 请求设置 `force_rag=true`；
- 后端即使路由模型判断不需要 RAG，也会强制走检索路径。

它适合调试路由或强制从知识库寻找证据。

---

## 六、Rerank：为什么召回以后还要精排

### 6.1 向量召回的问题

向量搜索速度快，但它通常只比较独立向量距离：

- 可能召回语义相近但不能回答问题的 chunk；
- 对精确关键词、数字、专有名词不一定敏感；
- top-5 过窄会漏召回，top-20 又会给 LLM 太多噪声。

经典做法是：

```text
快速粗召回 20 条
       ↓
更贵但更准的 Reranker
       ↓
保留 5 条
```

### 6.2 三种实现

#### NoOpReranker

禁用 Rerank，按向量结果直接取前 k。默认配置是 `none`。

#### CrossEncoderReranker

输入一组 `(query, document_text)`，让模型联合编码并输出相关性分数。

和双塔 embedding 的区别：

- Embedding：查询和文档分别编码，可提前存文档向量，检索快；
- Cross-Encoder：查询和文档一起看，交互更充分，通常更准，但每次查询都要计算。

工程措施：

- 首次使用才懒加载模型；
- 每个文档最多取前 1500 字符；
- 模型加载或预测失败时退回 vector-only；
- 候选数小于等于最终 top-k 时跳过；
- 记录 vector_score 和 rerank_score。

#### HybridFusionReranker

不用额外神经网络，把归一化向量分数与关键词覆盖分数融合：

```text
final_score = 0.7 × vector_score + 0.3 × keyword_score
```

优点是轻量；缺点是当前关键词评分只是简单 token overlap，不是完整 BM25。

### 6.3 Rerank 不是必然提升

项目评估结果出现过明显波动：

- 某次 Cross-Encoder 报告的 MRR、NDCG 有提升；
- 另一次同类实验 Recall、Precision、NDCG 反而下降；
- 当前 `baseline-vector-only.json` 的 Recall@5 约 0.682，而部分 rerank 报告低于它。

面试重点：

> Rerank 是一个候选方案，不是“加了就一定更好”。效果取决于候选召回质量、Rerank 模型领域匹配、文本截断、标注集和参数，所以必须通过同一 golden dataset 做 A/B 对比。

### 6.4 当前配置上的小细节

配置中有 `RERANKER_FINAL_TOP_K=5`，但生产 API 当前仍把 service 的 `top_k=5` 作为最终保留数，API 层也固定传 5。也就是说最终 top-k 目前并没有完全由配置驱动，这是一个可以继续重构的点。

---

## 七、上下文与长期记忆：重点必背

### 7.1 早期方案有什么问题

早期实现：

- 前端只发最近 10 条消息；
- 后端不根据 `conversation_id` 主动加载历史；
- 最终历史使用 `len(text) // 3` 粗略估算 2048 token；
- 文档、问题、系统提示和输出没有统一预算；
- 超出模型窗口后依赖底层服务报错。

问题是：

- SQLite 虽然保存了完整聊天，但模型并不会自动记住；
- 换客户端或只调用 API 时上下文行为不一致；
- 中文 token 估算不准；
- 历史没超限，不代表“历史 + 文档 + 问题”整体不超限；
- 长对话只能丢消息，没有长期语义保留。

### 7.2 当前方案是什么

现在使用：

```text
服务端持久化完整原始消息
          +
滚动摘要（旧消息）
          +
最近原文消息
          +
统一上下文预算
```

### 7.3 `conversation_id` 现在真正做了什么

每次请求：

1. 后端拿到 `conversation_id`；
2. 从 SQLite 读取 `summary`；
3. 读取 `summary_through_message_id` 之后的原始消息；
4. 与客户端尾部做最长重叠合并；
5. 使用合并后的服务端记忆进行路由和最终生成。

客户端仍发送最近 20 条，不是因为模型只记 20 条，而是解决一个竞态：

```text
上一轮 SSE 已经到达浏览器
  但后台 SQLite 保存可能还没有完成
用户马上发送下一轮
```

客户端尾部可以补上“已经显示、尚未落库”的消息。

### 7.4 为什么要最长重叠合并

不能简单把 SQLite 历史和客户端历史拼起来，否则会重复。

例如：

```text
SQLite: [Q1, A1, Q2, A2]
Client:          [Q2, A2, Q3, A3]
```

找到最长重叠 `[Q2, A2]` 后得到：

```text
[Q1, A1, Q2, A2, Q3, A3]
```

如果两边完全无重叠，以服务端历史为准，避免客户端污染或重复。

### 7.5 滚动摘要怎么工作

SQLite `conversations` 表新增：

```text
summary
summary_through_message_id
```

默认策略：

- 最近 12 条消息保留原文；
- 至少积累 8 条可压缩旧消息才触发；
- 旧消息与已有摘要交给 LLM 生成新的摘要；
- 摘要最大 1024 tokens；
- 原始 messages 不删除；
- 只推进 `summary_through_message_id`。

为什么不删除原文：

- UI 仍要展示完整聊天；
- 可以审计摘要是否丢信息；
- 更换摘要策略后可以重建；
- 避免不可逆的数据损失。

### 7.6 为什么摘要异步执行

摘要发生在回答保存后的后台线程中：

- 不增加当前请求首 token 延迟；
- 摘要失败不影响回答；
- 原始消息仍在，下次可以继续尝试；
- 代价是刚结束回答后立即发下一条时，可能暂时仍使用未摘要原文，所以才需要统一预算。

### 7.7 上下文窗口如何预算

最终可用输入：

```text
input_limit
= llm_context_window
- reserved_output_tokens
- safety_margin_tokens
```

默认：

```text
LLM_CONTEXT_WINDOW = 32768
LLM_RESERVED_OUTPUT_TOKENS = 2048
CONTEXT_SAFETY_MARGIN_TOKENS = 512
CONTEXT_HISTORY_MAX_TOKENS = 8192
CONTEXT_DOCUMENT_MIN_TOKENS = 4096
```

预算对象包括：

- RAG system prompt；
- 当前问题；
- 长期摘要；
- 最近消息；
- 文档内容及 Source header；
- 为回答预留的输出；
- tokenizer 偏差和消息协议开销。

### 7.8 裁剪优先级

1. 当前问题不静默裁剪；
2. 摘要保留有限预算；
3. 从最新向旧保留完整消息；
4. 最新单条消息仍过长时保留头尾；
5. 文档按检索顺序装入；
6. 最后一块文档可以截断；
7. 对完整渲染后的 prompt 再校验一次。

如果问题本身无法放入：

- 同步接口返回 HTTP 413；
- SSE 返回 `error`，其中 `phase=context`，然后发送 `done`。

### 7.9 Token 计数是否完全准确

默认是 `offline_multilingual`：

- 中文及非 ASCII 字符逐字符保守计数；
- ASCII 单词约按 3 字符一个 token；
- 标点单独计数；
- 不需要联网下载 tokenizer。

它比全局 `len/3` 更适合中文，但仍不是 Qwen 原生 tokenizer。

项目保留了 tiktoken 编码路径；如果编码表本地可用，可以配置 `cl100k_base` 等。但对于 Qwen，最佳方案仍是接入模型原生 tokenizer 或服务端 tokenize endpoint。

安全余量就是为了处理估算与真实 tokenizer 的差异。

### 7.10 一句话背诵上下文方案

> 我的上下文不是单纯截取最近 N 条，而是服务端按 conversation_id 恢复完整会话，用滚动摘要保存长期语义、近期消息保留原文，再对系统提示、问题、历史、检索文档和输出做统一 token 预算。

---

## 八、答案生成与 Prompt

### 8.1 Prompt 输入

最终 chain 输入包含：

- `question`：用户原问题；
- `context`：预算后的检索文档；
- `history`：滚动摘要和近期消息。

文档格式：

```text
[Source 1] source=xxx.md, page=3
<document content>

[Source 2] source=yyy.pdf, page=7
<document content>
```

### 8.2 Prompt 的核心约束

- 仅根据给定上下文回答；
- 找不到答案要明确说明；
- 不编造；
- 根据用户问题选择中文或英文；
- 默认使用自然连贯的段落；
- 有价值时自然提及来源。

### 8.3 为什么要返回 Sources

Sources 有三个作用：

- 可解释：用户知道答案从哪里来；
- 可验证：用户可以检查原文；
- 可调试：开发者可以判断是检索错了还是生成错了。

当前 SourceChunk 字段包括：

- content；
- source；
- file_name；
- file_path；
- chunk_index；
- page；
- score。

注意当前统一摄入链路没有显式写入 `metadata.chunk_index`，所以该字段在不少结果中可能为 `None`。Point ID 内部使用了顺序，但展示 metadata 尚需补齐，这是一个现存改进项。

---

## 九、SSE 流式输出

### 9.1 为什么不用普通 JSON 等完整回答

普通同步 JSON 必须等模型完成，用户可能长时间看不到反馈。

SSE 的价值：

- 服务端单向持续推送，适合 LLM token 流；
- 基于 HTTP，实现比 WebSocket 简单；
- 浏览器可以边接收边渲染；
- 可以携带阶段状态，不只是 token。

### 9.2 事件协议

| 事件 | 内容 | 用途 |
|------|------|------|
| `routing` | routing + conversation_id | 告诉前端走了哪条路径 |
| `status` | phase + message | searching/reranking/generating/responding |
| `token` | 字符串 | 逐步拼接答案 |
| `sources` | SourceChunk 数组 | 展示引用来源 |
| `error` | message + phase | 上下文或生成错误 |
| `done` | 空对象 | 流结束 |

### 9.3 修过的关键 Bug

前端 SSE parser 曾把 `currentEvent` 定义在每次 `reader.read()` 循环内部。

问题：

```text
网络 chunk 1: event: token
网络 chunk 2: data: "hello"
```

如果变量在下一次 read 时重置，就不知道 data 属于哪个 event，事件会丢失。

修复后 `currentEvent` 放在 while 外，跨网络 chunk 保持状态。

### 9.4 为什么后端所有 data 都 JSON 编码

早期字符串 token 不编码、对象才编码，前端需要依赖 JSON.parse 失败来区分，协议不稳定。

现在所有 data 都 `json.dumps`，前端统一 `JSON.parse`。

### 9.5 非 RAG 路径为什么也逐词推送

direct/greeting 的回答可能已经一次性得到。如果整段发送，UI 体验和 RAG 路径不一致。

项目将答案按词拆分，并用 `await asyncio.sleep(0)` 让出事件循环，使 ASGI 有机会 flush，不增加实际等待时间。

### 9.6 为什么 LLM 中途失败还要发 done

如果连接直接断开，前端可能永远停留在 loading。

当前生成异常会：

```text
error → sources → done
```

前端能明确结束状态，并显示错误。

---

## 十、会话持久化与数据存储

### 10.1 三类持久化数据

| 数据 | 存储 | 作用 |
|------|------|------|
| 文档向量与 payload | Qdrant | 语义检索 |
| 文件 MD5 摄入状态 | `data/ingestion_state.db` | 增量更新 |
| 对话、消息、摘要 | `data/conversations.db` | 历史列表与模型记忆 |

### 10.2 Conversation Store

主要表：

```text
conversations:
id, title, summary, summary_through_message_id,
created_at, updated_at

messages:
id, conversation_id, role, content,
sources, routing, created_at
```

特点：

- SQLite WAL 模式；
- foreign key + cascade delete；
- 模块级单例；
- 每次获取新 connection，WAL 支持并发读取；
- 会话标题取第一条用户问题前 60 字符；
- sources 以 JSON 保存；
- 老数据库缺摘要列时自动 `ALTER TABLE` 迁移。

### 10.3 为什么使用 SQLite

对于本地单用户项目：

- 零部署；
- 支持事务；
- 比 JSON 文件更适合并发和查询；
- 数据可持久化；
- Python 标准库自带。

如果变成多用户服务，可迁移到 PostgreSQL，并增加 user_id、权限和索引。

### 10.4 保存为什么是 Best-Effort

回答完成后通过 daemon thread 保存：

- 避免磁盘 I/O 阻塞响应；
- 保存失败记录日志，但不让用户丢失已经生成的回答；
- 代价是进程突然退出时极少数最后消息可能未写入。

商业系统可以改为任务队列、outbox 或可靠异步 job。

---

## 十一、文档管理与前端功能

### 11.1 文档 API

| 方法 | 路径 | 作用 |
|------|------|------|
| POST | `/documents/upload` | 单文件上传并摄入 |
| POST | `/documents/upload-batch` | 多文件逐个处理 |
| GET | `/documents` | 列出 source、类型、chunk 数 |
| DELETE | `/documents/{source}` | 删除 source 对应 Qdrant chunks |

批量上传始终返回 200，每个文件通过 `status=indexed/error` 表示结果，支持部分成功。

这种设计适合批处理，但如果 API 需要严格 REST 语义，也可以考虑 207 Multi-Status 或任务型接口。

### 11.2 文档列表如何实现

Qdrant 没有单独的 documents 表，因此通过 scroll 遍历 payload：

- 每批 500；
- 不读取 vectors；
- 按 `metadata.source` 聚合；
- 统计 chunk 数；
- 从文件后缀推断类型。

复杂度是 O(n)，文档量大时不适合每次实时扫描。更大规模应该增加独立文档元数据表。

### 11.3 删除文档的当前限制

上传文件本地以 UUID 文件名保存，Qdrant 的 source 是原始文件名。

当前删除通过 source 删除 Qdrant points 是准确的，但本地删除尝试 `data/raw/<source>`，通常找不到 UUID 文件，所以 `file_deleted` 可能是 false。

正确改进：

- 从 Qdrant metadata 先取真实 `file_path`；
- 或增加 documents 元数据表，保存 document_id、original_name、stored_path；
- 删除时用 document_id 做统一事务/补偿。

### 11.4 前端主要功能

- 健康检查；
- New Chat；
- 聊天消息与 Markdown 渲染；
- SSE 状态与流式 token；
- 来源卡片和相关性分数；
- 单/多文件上传；
- 文件列表、移除、逐文件状态；
- 文档管理列表与删除；
- 历史会话列表、切换、删除；
- `@rag` 强制检索；
- routing badge：Searched documents / Direct response / Quick reply。

### 11.5 前端做过的体验优化

- assistant 使用 `react-markdown`，用户消息保持纯文本，避免用户输入被解析；
- Markdown 覆盖标题、列表、代码、表格、引用和链接；
- CJK 排版改为 `word-break: normal`、`line-break: strict`，避免中文标点出现在行首；
- Editorial Ink 深色主题；
- 设计 token 统一颜色、阴影、圆角和过渡；
- 用户消息气泡、助手消息阅读式布局；
- 上传和文档管理 modal 支持 loading/error/empty/success 状态。

---

## 十二、Retrieval 评估体系

### 12.1 为什么不能只看最终答案

RAG 最终错误可能来自：

1. 没检索到证据；
2. 检索到了但排序太后；
3. 上下文噪声太多；
4. Prompt 没利用证据；
5. LLM 生成错误。

如果只看答案，无法判断是哪层出错。

因此先建立 Retrieval 层离线评估，把“找证据”的质量单独量化。

### 12.2 Golden Dataset

当前示例集：

- 22 条问题；
- 8 个主题；
- 标注 `file_path/source/file_name/text/relevance`。

为什么不直接标 chunk index：

- chunk_size 改了，index 会变化；
- Markdown 策略改了，边界会变化；
- 标注会失效。

所以标注稳定证据：

```text
file_path + 原文 snippet + relevance
```

匹配层再把这些标注映射到当前检索 chunk。

### 12.3 Recall@K

```text
Recall@K = top-K 中命中的相关证据数 / 全部相关证据数
```

回答“该找的东西找全了吗”。

高 Recall 对 RAG 很重要，因为证据没召回，生成模型无法凭空正确回答。

### 12.4 Precision@K

```text
Precision@K = top-K 中相关结果数 / K
```

回答“送给 LLM 的上下文有多少是有用的”。

低 Precision 会浪费 token、干扰模型，并增加错误引用风险。

### 12.5 MRR

```text
RR = 1 / 第一个相关结果排名
MRR = 多个问题 RR 的平均值
```

第一个相关结果排第 1，RR=1；排第 2，RR=0.5。

它关注“用户多快看到第一条有效证据”。

### 12.6 NDCG@K

NDCG 同时考虑：

- 排名位置；
- 分级相关性。

高价值证据排在前面得分更高。适合 relevant evidence 不只是 0/1，而是有 1–3 级重要性的场景。

### 12.7 上下文质量指标

- `irrelevant_rate@K`：不相关比例；
- `duplicate_rate@K`：重复上下文比例；
- `context_redundancy@K`：不相关 + 重复的综合比例，上限为 1。

### 12.8 评估 Pipeline

```text
读取 golden JSONL
  ↓
可选 query rewrite
  ↓
调用生产 retriever
  ↓
Document → RetrievedItem
  ↓
source/snippet 匹配
  ↓
计算指标
  ↓
输出 retrieval-eval-v1 JSON
  ↓
Notebook 加载多份报告对比
```

### 12.9 实验结果如何讲

历史实验曾比较 chunk=800/120 与 256/50。不同日志和报告批次的数字存在差异，因此面试不要死背某一个“最佳数字”，应说明实验环境和报告文件。

当前结果文件中可见：

- vector-only baseline 的 Recall@5 约 0.682；
- Precision@5 约 0.173；
- MRR 约 0.395；
- NDCG@5 约 0.340；
- context redundancy 约 0.827。

这个结果说明：

- 召回到部分正确证据，但 top-5 噪声仍然很高；
- duplicate rate 接近 0，主要问题是不相关，而不是 overlap 造成重复；
- 优化优先级应放在 embedding、query rewrite、候选策略和标注质量，而不是盲目加 overlap；
- Rerank 要做严格 A/B，不应默认宣称提升。

### 12.10 Notebook 有什么展示价值

Notebook 支持：

- 单题指标；
- 聚合均值与标准差；
- 雷达图；
- Top-K 敏感度；
- Recall vs Precision 权衡；
- 指标相关性热力图；
- 多实验配置表；
- 逐题敏感度热力图；
- DEMO/LIVE 两种模式。

面试时它证明你不仅会“实现”，也知道如何评估和做实验。

---

## 十三、迭代时间线：项目是怎么一步步长出来的

这一节是面试最重要的“项目演进故事”。

### 阶段 1：基础 RAG 闭环

目标：

- FastAPI 后端；
- 文档上传；
- Loader + Splitter；
- Embedding + Qdrant；
- `/rag/query`；
- React 聊天 UI。

面试价值：

> 先建立最小可用闭环，确保“文档进去、问题进来、答案出来”，然后再逐层解决真实工程问题。

### 阶段 2：前端排版与 Markdown

问题：

- 中文标点可能出现在行首；
- LLM 的 `**粗体**`、代码块等被当普通文本显示。

改进：

- 使用标准 CJK 换行；
- assistant 消息接入 `react-markdown`；
- 补全代码、表格、标题、列表等样式；
- 用户消息继续纯文本，避免意外解析。

学到的点：

> LLM 产品不只是后端模型质量，输出呈现会直接影响可读性和可信度。

### 阶段 3：多文件上传与文档管理

问题：

- 一次只能上传一个文件；
- 无法查看和删除已经索引的文档。

改进：

- 批量 multipart 上传；
- 每个文件独立返回成功或失败；
- 前端显示文件列表和逐文件状态；
- Qdrant scroll 聚合文档；
- 按 source 删除 chunks。

面试价值：

> 从 Demo 的单文件操作升级到更完整的知识库管理体验。

### 阶段 4：Editorial Ink UI 重构

问题：

- 初期 CSS 特效过多、重复代码多；
- 信息层级不清晰。

改进：

- CSS design tokens；
- 更克制的深色主题；
- 统一字体、间距、层级；
- 删除大量重复 CSS。

面试价值：

> 体现不是只增加功能，也会做可维护性和体验重构。

### 阶段 5：Pre-Retrieval Query Processing

问题：

- 用户原问题可能含糊；
- 直接 embedding 不一定适合搜索。

改进：

- LLM 意图识别；
- 查询改写；
- 改写用于检索、原问题用于生成；
- 失败回退到原问题。

后来进一步演进为“路由 + 意图 + 改写/直接回答”统一调用。

### 阶段 6：批量摄入与增量更新

问题：

- 大目录逐文件、逐 chunk 摄入慢；
- 重复运行会重复向量化；
- Markdown 结构丢失；
- Qdrant 大 payload 失败。

改进：

- CLI 递归扫描；
- MD5 checksum；
- 标题感知切分；
- 64 条批量 embedding；
- 500 points 分批 upsert；
- 统一 Web/CLI pipeline；
- collection 不存在自动创建。

这是项目从简单 Demo 走向工程化的重要一步。

### 阶段 7：检索评估体系

问题：

- “感觉回答不错”无法证明检索质量；
- chunk 和 embedding 调参没有统一标准。

改进：

- 22 条 golden question；
- 稳定 source/snippet 标注；
- Recall、Precision、MRR、NDCG；
- 上下文噪声和重复指标；
- CLI 报告和 Notebook 多策略对比。

### 阶段 8：中文注释、Notebook 展示和首轮调优

改进：

- 全项目中文文件头；
- 报告保存 `settings_snapshot`；
- 自动展示 chunk、embedding、collection；
- 修复 Notebook 路径和图表问题；
- 做 chunk 800/120 与 256/50 对比；
- 发现主要矛盾是 Precision 低和噪声高。

面试价值：

> 优化方向来自数据诊断，而不是主观猜测。

### 阶段 9：路由门控、多轮上下文和 SSE

问题：

- 所有请求固定走两次 LLM 和向量库；
- “你好”也查文档；
- 无多轮指代；
- 用户必须等待完整答案。

改进：

- Layer 0 正则快速回复；
- Layer 1 统一路由；
- conversation_id + history；
- SSE token streaming；
- embedding 缓存；
- 异步查询日志；
- `@rag` 强制检索。

### 阶段 10：Rerank

问题：

- top-5 直接向量搜索召回和精度难兼顾。

改进：

- 20 条粗召回；
- Cross-Encoder / Hybrid Fusion；
- 模型懒加载；
- 失败回退；
- 评估报告加入 rerank_config。

### 阶段 11：SSE 可靠性修复

问题：

- event/data 跨网络 chunk 时事件丢失；
- 搜索阶段长时间无反馈；
- direct 路径不流式；
- 模型异常导致 loading 卡死。

改进：

- 修复 `currentEvent` 生命周期；
- 统一 JSON 编码；
- status 事件；
- direct 逐词流；
- error + done 恢复。

### 阶段 12：会话持久化

问题：

- 刷新页面对话消失。

改进：

- SQLite conversations/messages；
- WAL；
- 会话列表、详情、删除 API；
- 前端侧边栏历史；
- 后台保存。

注意：这一阶段只是“记录持久化”，当时还不是完整模型记忆。

### 阶段 13：安全修复

问题：

- 云 API Key 曾作为源码默认值进入版本历史。

改进：

- 源码默认 Key 改为空；
- 只从 `.env` 或环境变量注入；
- `.env.example` 只留安全占位；
- 增加安全测试和仓库规则；
- 清理 Git 历史；
- 强调旧 Key 必须吊销，历史清理不能代替轮换。

面试价值：

> 能区分“把 Key 从最新代码删除”和“真正完成泄露响应”。

### 阶段 14：核心 API 自动化测试

问题：

- 早期很多测试只是占位；
- 修改路由容易回归。

改进：

- TestClient 驱动真实 FastAPI 路由；
- mock Qdrant/LLM/SQLite 边界；
- 覆盖 200/400/404/413/422/500；
- 覆盖 SSE headers 和事件；
- 测试可以离线、快速运行。

### 阶段 15：服务端长期记忆与统一上下文预算

问题：

- 前端最近 10 条不是长期记忆；
- SQLite 记录不等于模型上下文；
- 中文 token 粗估不准；
- 没有整体窗口预算；
- 长对话只丢旧消息。

改进：

- server-authoritative history；
- 客户端尾部最长重叠对账；
- rolling summary；
- recent verbatim messages；
- 路由与生成统一预算；
- 文档动态截断；
- 输出预留和安全余量；
- HTTP 413 / SSE context error；
- 65 项测试；
- 真实 LM Studio 生成与摘要冒烟验证。

### 一句话总结迭代逻辑

> 我的迭代路线是先打通功能闭环，再解决规模、质量、体验和可靠性问题：先能摄入和问答，再做批量与增量；再建立评估；然后优化路由、流式和 Rerank；最后补持久化、安全、测试和长期上下文。

---

## 十四、可靠性、安全与测试

### 14.1 失败处理

| 场景 | 当前处理 |
|------|----------|
| Query Processor 失败 | fail-open 到原问题 RAG |
| Cross-Encoder 加载失败 | fallback vector-only |
| Cross-Encoder 推理失败 | fallback vector-only |
| Conversation 读取失败 | 使用客户端历史尾部 |
| Conversation 保存失败 | 日志记录，不影响回答 |
| 摘要失败 | 保留原消息，后续再尝试 |
| LLM 流式中断 | SSE error + done |
| 问题超过上下文 | 同步 413 / SSE context error |
| 单个批量上传失败 | 记录该文件 error，其他文件继续 |
| Qdrant collection 被清空 | 首次 upsert 自动创建 |

### 14.2 安全要点

- Cloud API Key 源码默认值为空；
- `.env` 被忽略；
- `.env.example` 不允许真实密钥；
- 上传文件以 UUID 命名，降低路径冲突；
- 扩展名白名单；
- Pydantic 限制 question 和 message 长度；
- 已泄露密钥必须吊销并清理 Git 历史。

尚未解决：

- 没有用户认证和会话授权；
- `conversation_id` 不是安全凭证；
- 没有文件内容病毒扫描；
- 没有 prompt injection 防御层；
- CORS 只适合当前本地前端配置；
- 文档原文与日志的敏感信息治理还可加强。

### 14.3 自动化测试

当前 65 项 unittest，覆盖：

- root/health/OpenAPI；
- 文档上传、批量部分失败、列表、删除；
- RAG 请求默认值、参数校验、500、413；
- SSE Content-Type、headers 和事件；
- 会话列表、详情、删除；
- 配置安全默认值；
- Retrieval metrics、matching、evaluator；
- 上下文历史合并、预算、截断、摘要、旧库迁移。

测试策略：

- API 层使用 FastAPI TestClient；
- 外部 Qdrant、模型和存储边界使用 mock；
- 纯指标函数使用真实输入输出；
- 另做本地 LM Studio 冒烟测试验证真实生成。

当前缺少：

- Docker Qdrant 集成测试；
- 真实 embedding + Qdrant + LLM 端到端 CI；
- 前端组件测试和浏览器自动化；
- 长对话事实保持率评估；
- 生成答案忠实度评估。

---

## 十五、项目亮点怎么讲

### 亮点 1：不是只有 RAG 主链路，还有摄入工程

关键词：

- 批量 embedding；
- MD5 增量；
- 幂等；
- 结构化 Markdown；
- Qdrant payload 限制处理；
- stable point ID；
- Web/CLI pipeline 复用。

### 亮点 2：动态路由降低不必要成本

关键词：

- 规则层零调用；
- LLM 层统一决策；
- 0/1/2 次模型调用；
- fail-open；
- `@rag` 调试入口。

### 亮点 3：上下文机制有长期记忆与统一预算

关键词：

- server-authoritative；
- rolling summary；
- recent verbatim；
- race reconciliation；
- whole-prompt budgeting；
- output reserve；
- safety margin；
- deterministic truncation。

### 亮点 4：有检索评估闭环

关键词：

- golden dataset；
- source/snippet stable label；
- Recall/Precision/MRR/NDCG；
- redundancy；
- strategy comparison；
- 实验驱动，不盲目调参。

### 亮点 5：流式协议不是简单 token 拼接

关键词：

- routing/status/token/sources/error/done；
- network chunk 边界；
- JSON consistency；
- mid-stream recovery；
- perceived latency。

### 亮点 6：工程可靠性

关键词：

- fallback；
- WAL；
- background persistence；
- schema migration；
- secret management；
- 65 offline tests；
- local model smoke test。

---

## 十六、高频面试问题与参考回答

### Q1：为什么要用 RAG，不直接微调？

> RAG 更适合频繁变化、需要引用来源的知识。新增或修改文档只需要重新摄入相关文件，不需要重新训练；文档也能保留在本地。微调更适合改变模型风格、格式或稳定行为，不适合让模型精确记住频繁更新的事实。这个项目也需要可追溯来源，所以优先选择 RAG。

### Q2：RAG 最重要的环节是什么？

> 很多时候不是生成，而是 Retrieval。如果正确证据没有进入上下文，模型再强也无法可靠回答。因此我单独建立了 Recall、Precision、MRR、NDCG 和上下文噪声评估。

### Q3：为什么 chunk_size 不是越小越好？

> 小 chunk 定位更精确，但容易丢语义和章节上下文；大 chunk 更完整，但向量可能混合多个主题并浪费 token。项目实验也出现小 chunk 提升某些召回指标但降低 Precision/NDCG 的情况，所以需要在固定 golden dataset 上比较。

### Q4：为什么需要 overlap？

> 防止答案刚好跨分块边界。但 overlap 会增加存储和重复，因此不能无限增大。项目的重复率实验接近 0，说明当前主要问题不是 overlap，而是不相关结果。

### Q5：为什么 Markdown 采用标题感知切分？

> 工程文档的标题本身就是语义。先按 H1/H2/H3 切，可以让 chunk 保留章节层级，再对超长章节递归切分，比纯字符切分更不容易把标题和正文分离。

### Q6：为什么用 Qdrant？

> 它是专门的向量数据库，支持 cosine 相似度、payload metadata、过滤、scroll、count 和 delete；既能做语义检索，也能按 file_path/source 管理文档。Docker 本地部署也符合本地优先目标。

### Q7：Qdrant 存了什么？

> 每个 point 包含 embedding vector，以及 page_content 和 metadata。metadata 包括 source、file_path、file_name、file_type、MD5、标题层级、页码等。

### Q8：为什么用 cosine？

> 文本 embedding 主要关心向量方向的语义相似性，cosine 对向量绝对长度不敏感，是常见选择。前提是 embedding 模型和 Qdrant collection 配置一致。

### Q9：增量摄入如何保证幂等？

> 文件内容计算 MD5；未变化直接跳过，变化则按 file_path 删除旧 chunks 后重新写。Point ID 又由 file_path、MD5 和顺序稳定生成，所以重复摄入同一内容不会无限新增不同 ID。

### Q10：如果文件更新后 chunk 数减少怎么办？

> 先删除该 file_path 的全部旧 points，再写新 chunks，避免残留旧 chunk。只做 upsert 不足以处理 chunk 数减少。

### Q11：为什么批量 embedding？

> 减少 HTTP 往返，更好利用服务端 batch 能力。项目默认每批 64 条；响应按 index 排序避免向量和 chunk 错位。

### Q12：为什么 Qdrant upsert 分 500 一批？

> 实测 809 个 point 的单请求超过 Qdrant 32MB payload 限制并返回 400，所以选择 500 作为安全批大小。这个数字来自故障验证，不是随意设置。

### Q13：Query Rewrite 会不会改变用户意图？

> 有风险，所以改写只用于检索，最终生成仍使用用户原问题。还保留原问题 fallback，并把会话摘要和近期消息用于指代消解。

### Q14：路由错误怎么办？

> 规则层只处理非常明确的问候等表达；不确定时 LLM 默认倾向 RAG。路由模型异常时 fail-open 到 RAG 原问题，避免因为路由故障漏掉知识库证据。

### Q15：为什么路由、意图和改写放在一次 LLM 调用？

> 三个任务共享对问题的理解，合并可以减少延迟和 token 成本。通过固定输出格式解析 ROUTING、INTENT、QUERY 或 ANSWER。

### Q16：Rerank 和向量检索有什么区别？

> 向量检索是双塔结构，文档向量可预计算，适合大规模快速召回；Cross-Encoder 联合看 query 和 doc，交互更细但计算贵，适合对少量候选精排。

### Q17：为什么 Rerank 可能让结果变差？

> Rerank 模型可能与领域不匹配，候选中可能根本没有正确证据，文档截断可能丢失关键信息，参数和标注也会影响结果。所以要用相同数据集做 A/B，而不是只看单个问题。

### Q18：为什么使用 SSE 而不是 WebSocket？

> 当前需求是服务端单向推送 token 和状态，不需要双向实时通信。SSE 基于 HTTP、实现更简单、调试方便，足够满足 LLM streaming。若未来需要实时协作或工具交互，再考虑 WebSocket。

### Q19：SSE 如何处理粘包/拆包？

> `reader.read()` 得到的是网络 chunk，不保证等于一条 SSE event。前端维护 buffer，只处理完整行，把不完整尾部留到下一次；`currentEvent` 必须跨 read 保留，直到对应 data 到来。

### Q20：上下文为什么不能按消息条数截断？

> 一条消息可能 10 个 token，也可能 10000 个。按条数不能保证模型窗口。当前系统按 token 预算，并把系统提示、问题、文档、输出和安全余量一起计算。

### Q21：长期上下文怎么处理？

> 完整消息保存在 SQLite；模型侧采用滚动摘要保存旧消息语义，近期 12 条保留原文。每次请求由服务端按 conversation_id 恢复，再统一预算。

### Q22：摘要会不会丢信息？

> 会，这是摘要记忆的固有限制。所以项目保留完整原文、不物理删除；摘要提示词要求保留目标、实体、决策、约束和未解决问题；后续还应增加摘要忠实度评估和可重建机制。

### Q23：为什么上下文需要安全余量？

> 不同模型 tokenizer 和 ChatML 消息开销不同，离线估算不可能完全一致。安全余量降低预算刚好顶到窗口导致请求失败的概率。

### Q24：为什么问题过长不直接截断？

> 当前问题代表用户真实意图，静默截断可能改变语义。系统宁可明确返回 413，让调用方缩短输入；历史和文档则可以按策略压缩或截断。

### Q25：Conversation ID 是否安全？

> 目前不是。它只是会话定位 ID，没有认证和权限。多用户环境必须增加 user_id、鉴权、行级权限和不可枚举标识。

### Q26：为什么聊天使用 SQLite 而不是 Qdrant？

> 对话是有序、结构化、需要事务和 CRUD 的数据，关系型 SQLite 更合适；Qdrant 用于语义向量检索。不同存储按数据访问模式选择。

### Q27：为什么保存和日志放后台线程？

> 它们不影响当前答案内容，可以移出请求关键路径降低响应等待。缺点是进程突然退出可能丢最后一次写入，商业系统应使用可靠队列。

### Q28：如何判断回答错在 Retrieval 还是 Generation？

> 先查看返回 sources。如果正确证据没出现，是 Retrieval 问题；正确证据出现但答案仍错，是 Prompt/Generation 问题。查询日志记录改写词、结果、分数和答案，评估体系也单独量化 Retrieval。

### Q29：Recall 和 Precision 哪个更重要？

> RAG 首先要保证证据能被召回，所以 Recall 是底线；但 Precision 太低会造成上下文污染。常见做法是扩大粗召回保证 Recall，再用 Rerank 和上下文预算提高 Precision。

### Q30：MRR 和 NDCG 有什么区别？

> MRR 只看第一个相关结果的位置；NDCG 看整个 top-k 排序，并支持不同相关性等级。一个适合首个有效结果，一个适合整体排序质量。

### Q31：为什么 golden 标注用 snippet，不直接用文件名？

> 一个文件可能包含很多主题，只按文件名命中会过宽。file_path + snippet 可以验证具体证据，同时比 chunk index 更稳定。

### Q32：当前项目最大的技术不足是什么？

建议诚实回答：

> 当前 Retrieval Precision 仍低，说明检索噪声较大；默认离线 token 计数不是 Qwen 原生 tokenizer；没有用户鉴权；文档元数据管理还依赖 Qdrant scroll；上传文件 UUID 路径与原文件名删除没有完全打通；缺少真实端到端 CI 和生成质量评估。这些都是我下一步会优先改的。

### Q33：如果数据量扩大 100 倍怎么办？

> 文档列表不能再全量 Qdrant scroll，应建立 PostgreSQL 文档表；摄入改为异步任务队列；Qdrant 使用更合理的 collection/index 配置；embedding 并发和批处理要限流；会话用 PostgreSQL/Redis；增加 metrics、trace 和重试队列。

### Q34：如果多人同时上传同一文件怎么办？

> 当前 checksum store 和文件级流程还不是完整的分布式幂等。可以用 content hash + collection 作为唯一键，在数据库事务中抢占任务，或用分布式锁和任务状态机避免重复处理。

### Q35：如何防 Prompt Injection？

> 当前 prompt 强调只基于上下文，但还不是完整防御。应将检索内容视为不可信数据，明确分隔，禁止执行文档中的指令；增加输入/检索内容安全分类、权限过滤、工具调用隔离和输出审计。

### Q36：为什么项目用了 AI 辅助开发，面试如何诚实表达？

> 可以明确说项目大量使用了 AI 辅助编码，但自己负责需求拆解、架构选择、验证、故障定位、实验和迭代决策。不要声称手写了每一行。面试重点是你是否能解释数据流、设计取舍、失败模式，并能在现场修改和调试。

### Q37：你在项目中真正学到了什么？

参考结构：

> 我学到 RAG 不是“向量库 + Prompt”这么简单。真正困难的是数据摄入一致性、检索质量评估、上下文预算、流式协议边界、失败回退和可观测性。尤其评估结果让我意识到加 Rerank 或减小 chunk 不一定更好，必须靠数据验证。

### Q38：如果只给你一周继续优化，你做什么？

优先顺序：

1. 修复文档元数据表、stored_path 删除和 chunk_index；
2. 用 Qwen 原生 tokenizer 替换估算；
3. 基于现有 golden dataset 系统比较 embedding/query rewrite/rerank；
4. 增加真实 Qdrant + LM Studio 集成测试；
5. 增加生成忠实度和引用正确性评估；
6. 增加认证与 conversation ownership。

---

## 十七、当前不足与改进路线

### P0：影响正确性

- Retrieval Precision 和上下文冗余仍不理想；
- Rerank 实验不稳定；
- 默认 token 计数不是模型原生 tokenizer；
- 文档删除不能稳定删除 UUID 落盘文件；
- chunk_index metadata 可能缺失；
- 无权限过滤，任何可访问服务的人可能读取会话或文档。

### P1：影响工程化

- 文档管理通过 Qdrant 全量 scroll，规模大时成本高；
- 后台 daemon thread 不是可靠任务队列；
- embedding 缓存不是严格 LRU，也不跨进程；
- reranker 单例不会响应运行时配置变化，需重启；
- top-k 仍在 API 层固定为 5，配置没有完全统一；
- 批量上传串行处理，吞吐有限；
- 文件读取一次性 `await file.read()`，大文件内存压力高。

### P2：影响评估与展示

- 只有 Retrieval 评估，缺生成忠实度、答案正确率和引用准确率；
- golden dataset 规模较小；
- 历史报告有不同批次数字，需要实验版本管理；
- 前端缺自动化组件测试；
- 缺 Prometheus/OpenTelemetry 监控。

### 推荐演进架构

```text
PostgreSQL: users/documents/conversations/jobs
Redis: cache/rate-limit/task state
Task Queue: ingestion + summary
Qdrant: vectors only
Object Storage: original files
OpenTelemetry: trace each RAG step
Evaluation: retrieval + faithfulness + answer correctness
Auth: user/document/conversation ownership
```

---

## 十八、模块速查表

### 后端入口与 API

| 文件 | 你要怎么解释 |
|------|--------------|
| `app/main.py` | 创建 FastAPI、CORS、日志、注册四组路由 |
| `app/api/health.py` | 健康检查 |
| `app/api/documents.py` | 上传、批量上传、列表、删除 |
| `app/api/rag.py` | 同步 RAG 和 SSE RAG |
| `app/api/conversations.py` | 会话列表、详情、删除 |

### Service 编排层

| 文件 | 责任 |
|------|------|
| `ingestion_service.py` | 把上传文件交给统一摄入 pipeline，并保留原文件名 |
| `rag_service.py` | 恢复记忆、路由、检索、Rerank、预算、生成、SSE、日志、保存 |
| `document_service.py` | 文档列表和删除业务 |

### RAG 原语层

| 文件 | 责任 |
|------|------|
| `loader.py` | 按扩展名选择 Loader |
| `splitter.py` | Markdown 标题切分 + 递归切分 |
| `embeddings.py` | 查询 embedding 和 256 条内存缓存 |
| `vectorstore.py` | Qdrant 检索实例、列表和 source 删除 |
| `retriever.py` | similarity_search_with_score |
| `query_processor.py` | 两层路由、意图、改写、直接回答 |
| `reranker.py` | NoOp/CrossEncoder/HybridFusion |
| `context_manager.py` | 服务端记忆、历史合并、token 预算、摘要 |
| `conversation_store.py` | SQLite 会话和摘要游标 |
| `prompt.py` | 最终 RAG 系统提示 |
| `chain.py` | LCEL 同步 invoke 和异步 astream |
| `query_logger.py` | JSONL 查询轨迹和终端摘要 |

### 摄入子模块

| 文件 | 责任 |
|------|------|
| `checksum_store.py` | SQLite MD5 状态 |
| `batch_embedder.py` | 64 条批量 embedding |
| `bulk_writer.py` | 500 points upsert、collection 自动创建、按路径删除 |
| `ingest_pipeline.py` | 扫描、分类、加载、切分、向量化、写入 |

### 评估模块

| 文件 | 责任 |
|------|------|
| `metrics.py` | Recall/Precision/MRR/NDCG/冗余 |
| `matching.py` | golden source/snippet → 当前 chunk |
| `evaluator.py` | 单案例统一评估输出 |
| `run_retrieval_eval.py` | 生产 retriever CLI 实验 |
| `retrieval_eval_pipeline.ipynb` | 可视化和多策略比较 |

### 前端

| 文件 | 责任 |
|------|------|
| `App.jsx` | 所有 UI 状态和交互 |
| `api.js` | Axios API + fetch SSE parser |
| `App.css` | 组件和设计系统 |
| `index.css` | reset、字体、全局纹理 |

---

## 十九、关键数字速记

| 数字 | 含义 |
|-----:|------|
| 4 | FastAPI 路由模块：health/documents/rag/conversations |
| 5 | 默认最终 top-k |
| 20 | Rerank 候选数；前端会话对账尾部条数 |
| 22 | Golden retrieval questions |
| 8 | Golden dataset 主题数；摘要批处理旧消息阈值 |
| 12 | 长期记忆保留的近期原文消息数 |
| 64 | 默认 embedding batch size |
| 65 | 当前离线 unittest 数 |
| 120 | 默认 chunk overlap |
| 256 | 查询 embedding 缓存上限 |
| 500 | Qdrant upsert batch size、scroll batch size |
| 800 | 默认 chunk size |
| 1024 | 滚动摘要最大 token |
| 1500 | Cross-Encoder 单文档最大字符 |
| 2048 | 生成输出预留 tokens |
| 4096 | 路由历史上限 / 文档最低预留 |
| 8192 | 最终历史上限 |
| 32768 | 默认 LLM context window |

---

## 二十、简历描述参考

请根据自己真实参与程度调整，不要声称未做过的工作。

### 版本 A：偏后端/RAG

> 构建本地优先的 RAG 知识问答系统，基于 FastAPI、LangChain、Qdrant 和 OpenAI-compatible 本地模型完成文档摄入、查询改写、向量检索、可插拔 Rerank 与 SSE 流式生成；设计 MD5 增量摄入和批量 embedding/upsert，将 Web 上传与 CLI 摄入统一为同一 Pipeline。

### 版本 B：偏评估

> 建立 RAG Retrieval 离线评估体系，使用 22 条 source/snippet 级 golden questions 和 Recall@K、Precision@K、MRR、NDCG、上下文冗余率评估 chunk、embedding 与 Rerank 策略，并通过 Notebook 自动生成多实验对比与逐题敏感度分析。

### 版本 C：偏上下文与工程化

> 实现服务端长期会话记忆：SQLite 持久化完整消息，滚动摘要压缩旧历史并保留近期原文；统一预算系统提示、问题、对话、检索文档、输出预留和安全余量，支持确定性裁剪、HTTP 413/SSE 错误恢复和旧数据库自动迁移。

### AI 辅助开发的诚实表述

> 项目使用 AI 辅助生成了大量代码，我主要负责需求拆解、架构梳理、验证、问题定位、实验和迭代决策。目前我通过代码链路、DevLog、测试和真实模型验证逐步建立对项目的完整理解。

---

## 二十一、背诵与复习计划

### 第一天：只掌握主线

背会：

- 30 秒介绍；
- 摄入链路；
- 查询链路；
- 为什么 RAG。

目标：不看文档完整讲 3 分钟。

### 第二天：上下文与流式

重点：

- conversation_id；
- summary + recent messages；
- token 预算公式；
- SSE 事件和 currentEvent bug。

目标：回答“上下文怎么处理”和“SSE 怎么做”。

### 第三天：检索与 Rerank

重点：

- embedding；
- cosine；
- top-k；
- Cross-Encoder；
- Recall/Precision/MRR/NDCG。

目标：能解释为什么 Rerank 不一定提升。

### 第四天：摄入与存储

重点：

- Loader；
- Markdown split；
- MD5；
- batch embedding；
- 500 points；
- SQLite/Qdrant 分工。

### 第五天：迭代故事

按顺序讲：

```text
基础闭环
→ 交互体验
→ 批量与增量
→ 评估
→ 路由与流式
→ Rerank
→ 持久化
→ 安全与测试
→ 长期上下文
```

### 第六天：不足与系统设计

练习：

- 数据扩大 100 倍；
- 多用户权限；
- 任务队列；
- Prompt Injection；
- 评估扩展；
- 为什么不用微调。

### 第七天：模拟面试

规则：

1. 每题先给结论；
2. 再讲当前实现；
3. 再讲为什么；
4. 最后讲不足和改进。

推荐答题模板：

> 当前项目是怎么做的 → 为什么这么设计 → 实际遇到什么问题/数据 → 有什么限制 → 下一步怎么改。

---

## 二十二、最后的自测清单

不看文档，确认你能回答：

- [ ] 这个项目解决什么问题？
- [ ] RAG 和微调的区别？
- [ ] 一份 PDF 如何变成 Qdrant point？
- [ ] 为什么 Markdown 要标题感知切分？
- [ ] MD5 增量更新如何避免旧 chunk 残留？
- [ ] 为什么 embedding 和 upsert 要批量？
- [ ] 查询为什么需要改写？
- [ ] 三条路由路径各有几次 LLM 调用？
- [ ] 向量检索和 Cross-Encoder 的区别？
- [ ] 为什么 Rerank 不保证提升？
- [ ] 上下文现在如何恢复？
- [ ] 滚动摘要如何触发？
- [ ] 为什么保留完整原文？
- [ ] 上下文预算公式是什么？
- [ ] 为什么要输出预留和安全余量？
- [ ] SSE 有哪些事件？
- [ ] `currentEvent` Bug 是什么？
- [ ] SQLite 和 Qdrant 分别存什么？
- [ ] Recall、Precision、MRR、NDCG 各自衡量什么？
- [ ] 为什么 golden dataset 不标 chunk index？
- [ ] 当前最明显的 5 个不足是什么？
- [ ] 数据量扩大后如何演进？
- [ ] 如何诚实说明 AI 辅助开发？
- [ ] 你在这个项目里真正学到了什么？

如果这些问题都能脱稿回答，这个项目就不再只是“AI 写出来能运行的代码”，而是你能够解释、验证和继续改进的项目。
